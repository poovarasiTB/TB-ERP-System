# ===========================================
# TB ERP System - Consolidated Environment Configuration
# ===========================================

ENVIRONMENT=development
DEBUG=True

# Shared Database Credentials
DB_NAME=tb_erp_db
DB_HOST=dpg-ctlpcvrqf0us7389o680-a.singapore-postgres.render.com
# Local DB fallback: localhost (for direct python running) or postgres (for docker)
# DB_HOST=localhost 

DB_PASSWORD=your_secure_password
DB_PORT=5432
DB_USER=admin

# Prisma Connection URL (for Frontend)
DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}?schema=auth

# Security Secrets (MUST match across all services)
JWT_SECRET=your_32_char_signing_key
NEXTAUTH_SECRET=your_32_char_session_key

# Service URLs
ASSET_SERVICE_URL=http://localhost:8001
INVOICE_SERVICE_URL=http://localhost:8002
EMPLOYEE_SERVICE_URL=http://localhost:8003
NEXTAUTH_URL=http://localhost:3000

# Cache
REDIS_URL=redis://localhost:6379
